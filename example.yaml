name: run
script: |
  pwd
  echo $jobid $jobndx
  $mpirun hostname

backend: defiant
resources:
  duration: 30
  node_count: 2
  processes_per_node: 4
  cpu_cores_per_process: 16
#  gpu_cores_per_process: 1
# translates to:
# salloc -t30 -N2 --ntasks-per-node 4 -c16 --gpus-per-task 1
# salloc -t30 -N2 --ntasks-per-node 4 -c16
